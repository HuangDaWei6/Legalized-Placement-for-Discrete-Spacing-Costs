#!bin/bash
# $1 # get argv 1

path="/home/user/dwhuang/CUDA_WORKS/gloden_bench/"
for file in *; do
    if [ -d "$file" ]; then
        base_name=$(basename "$file")
        # echo "$base_name"
        echo "$file"
        cd "$file"
        echo "${path}${base_name}.def"
        cp "${path}${base_name}.def" .
        # for non_lef_file in *; do
            # base_name = 1
            # if [ -f "$non_lef_file" ] && [[ "$non_lef_file" != *.lef ]]; then
            #     rm $non_lef_file
            # fi            
        # done
        cd ..
    fi
done

# make
# clear
# path="./hw/hw5/tests/sat.dofile"  
# # echo "gv-ref"
# # ./gv-ref -file $path
# echo "========================================================================"
# echo "gv"
# ./gv -file $path




# if [ "$1" == "ref" ]; then
#     exe="./gv-ref"
#     echo "gv-ref"
# else
#     echo "gv"
#     exe="./gv"
# fi

# for file in "$path"/*; do
#     if [ -f "$file" ] && [ "$(basename "$file")" != "ExeAllFiles" ]; then
#         $exe -file "$file"
#     fi
# done
